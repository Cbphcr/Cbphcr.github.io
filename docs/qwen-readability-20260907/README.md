# 通俗化笔记与配图检查

2026-09-07：总览与七章改为例子、图解、通俗解释、发展主线；原有详细内容保存在原生 details 展开区。配图使用 HTML/CSS，文字可选择，手机端重新排版。

逐张查看八张图的 1440px 与 390px 浏览器截图。修复全局 figure flex 样式导致的卡片宽度异常，去掉 GDN 旧状态的删除线，避免暗示历史全部清空。

语义检查：Embedding 标明假设分词、向量查表与第二层汇合；GDN 状态与 QSA 历史访问区分；RMSNorm 示例注明 gain、epsilon 和条形图比例；MoE 示意数量与实际配置区分；MTP 限定贪心教学例子；残差四分支不表示四次子层执行；Muon 示意更新方向而非权重矩阵。

验证：Jekyll build、git diff --check、JavaScript 语法检查通过。九页 449 个笔记内部链接目标检查通过。八页在桌面与手机宽度下无横向溢出，配图无越界，进阶区默认关闭、空格键可展开，展开后无公式渲染错误。checks.json 保存浏览器测量。系统深色偏好下网站沿用原有浅色主题。

保留 Embedding 桌面与手机截图供复核。本轮发布前另补充 N-gram 整数计算、表项训练与碰撞示例，并将七章调整为面向熟悉 Transformer / LLaMA 的读者。示例统一使用南京大学。
